version: '3'
services:
  #nginx:
  #  image: nginx
  #  ports:
  #    - "80:80"
  #  volumes:
   #   - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #  links:
     # - backend
    #  - app
   #   - admin
  #  restart: on-failure:5
 
  admin:
    image: ria/admin
    ports:
      - "8080:8080"
    restart: on-failure:5

  backend:
    image: ria/backend
    ports:
      - "3000:3000"
    restart: on-failure:5

  #app:
  #  image: ria/app
  #  ports:
  #    - "3001:3001"
  #  restart: on-failure:5
